spring:
  ai:
    model:
      embedding: ollama
    ollama:
      embedding:
        options:
          model:
        enabled: true
        model: ollama
      base-url: http://localhost:11434
      chat:
        options:
          model: llama3:2
      init:
        pull-model-strategy: always
  #      embedding:
   #       additional-models:
  #          - mxbai-embed-large
  #          - nomic-embed-text